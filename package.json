{
  "private": true,
  "scripts": {
    "build": "npm-run-all -s build:slide build:index",
    "build:slide": "marp -c marp.config.ts",
    "build:index": "bash script.sh > dist/index.html",
    "clean": "rimraf dist",
    "dev": "marp -s .",
    "fmt": "sort-package-json"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.3.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.5",
    "sort-package-json": "^2.6.0",
    "typescript": "^5.2.2"
  }
}
