{
  "private": true,
  "scripts": {
    "build": "npm-run-all -s build:slide build:index",
    "build:slide": "marp -c marp.config.ts",
    "build:index": "ts-node src/index.ts",
    "clean": "rimraf dist",
    "dev": "marp -s .",
    "fmt": "sort-package-json"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.3.0",
    "@tsconfig/node-lts": "^20.0.0",
    "@types/mustache": "^4.2.3",
    "@types/node": "^20.8.5",
    "glob": "^10.3.10",
    "mustache": "^4.2.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.5",
    "sort-package-json": "^2.6.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
