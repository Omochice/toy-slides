{
  "private": true,
  "scripts": {
    "build": "marp -c marp.config.ts",
    "clean": "rimraf dist",
    "dev": "marp -s .",
    "fmt": "sort-package-json"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "^3.3.0",
    "rimraf": "^5.0.5",
    "sort-package-json": "^2.6.0",
    "typescript": "^5.2.2"
  }
}
